import { View } from "react-native"

export const CharacterInvite = ({route, navigation}) => {

    const fetchInvites = (userid) => {
        
    }

    return (
        <View>
            
        </View>
    )
}